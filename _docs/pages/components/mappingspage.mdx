# MappingsPage

The `MappingsPage` component.

## Usage

```tsx
import { MappingsPage } from '...'

<MappingsPage
  id="value"
  name="value"
  type={/* 'income' | 'expense' */}
>
  {/* children */}
</MappingsPage>
```

## Props

| Prop | Type | Default | Required | Description |
|---|---|---|---|---|
| `id` | `string` | — | Yes | — |
| `name` | `string` | — | Yes | — |
| `type` | `'income' \| 'expense'` | — | Yes | — |
