# SettingsPage

The `SettingsPage` component.

## Usage

```tsx
import { SettingsPage } from '...'

<SettingsPage
  code="value"
  symbol="value"
  name="value"
>
  {/* children */}
</SettingsPage>
```

## Props

| Prop | Type | Default | Required | Description |
|---|---|---|---|---|
| `code` | `string` | — | Yes | — |
| `symbol` | `string` | — | Yes | — |
| `name` | `string` | — | Yes | — |
