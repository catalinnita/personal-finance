# /budgets

## GET /api/budgets

Handle `GET` requests to `/api/budgets`.


### Example

```bash
curl -X GET http://localhost:3000/api/budgets
```

---

## POST /api/budgets

Handle `POST` requests to `/api/budgets`.


### Example

```bash
curl -X POST http://localhost:3000/api/budgets
```

---

## DELETE /api/budgets

Handle `DELETE` requests to `/api/budgets`.


### Example

```bash
curl -X DELETE http://localhost:3000/api/budgets
```
