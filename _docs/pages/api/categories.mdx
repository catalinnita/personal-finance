# /categories

## PUT /api/categories/[id]

Handle `PUT` requests to `/api/categories/[id]`.


### Example

```bash
curl -X PUT http://localhost:3000/api/categories/[id]
```

---

## DELETE /api/categories/[id]

Handle `DELETE` requests to `/api/categories/[id]`.


### Example

```bash
curl -X DELETE http://localhost:3000/api/categories/[id]
```

---

## GET /api/categories

Handle `GET` requests to `/api/categories`.


### Example

```bash
curl -X GET http://localhost:3000/api/categories
```

---

## POST /api/categories

Handle `POST` requests to `/api/categories`.


### Example

```bash
curl -X POST http://localhost:3000/api/categories
```

---

## PUT /api/categories

Handle `PUT` requests to `/api/categories`.


### Example

```bash
curl -X PUT http://localhost:3000/api/categories
```
