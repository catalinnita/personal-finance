# /transactions

## PUT /api/transactions/[id]

Handle `PUT` requests to `/api/transactions/[id]`.


### Example

```bash
curl -X PUT http://localhost:3000/api/transactions/[id]
```

---

## DELETE /api/transactions/[id]

Handle `DELETE` requests to `/api/transactions/[id]`.


### Example

```bash
curl -X DELETE http://localhost:3000/api/transactions/[id]
```

---

## GET /api/transactions

Handle `GET` requests to `/api/transactions`.


### Example

```bash
curl -X GET http://localhost:3000/api/transactions
```

---

## POST /api/transactions

Handle `POST` requests to `/api/transactions`.


### Example

```bash
curl -X POST http://localhost:3000/api/transactions
```
