# CloseButton

The `CloseButton` component.

## Usage

```tsx
import { CloseButton } from '...'

<CloseButton
  onClick={() => {}}
  className="value"
  xClassName="value"
>
  {/* children */}
</CloseButton>
```

## Props

| Prop | Type | Default | Required | Description |
|---|---|---|---|---|
| `onClick` | `() => void` | — | Yes | — |
| `className` | `string` | — | Yes | — |
| `xClassName` | `string` | — | Yes | — |
