# SectionCard

The `SectionCard` component.

## Usage

```tsx
import { SectionCard } from '...'

<SectionCard
  className="value"
  hClassName="value"
  label="value"
>
  {/* children */}
</SectionCard>
```

## Props

| Prop | Type | Default | Required | Description |
|---|---|---|---|---|
| `className` | `string` | — | Yes | — |
| `hClassName` | `string` | — | Yes | — |
| `label` | `string` | — | Yes | — |
| `children` | `ReactNode` | — | Yes | — |
