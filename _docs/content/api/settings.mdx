# /settings

## GET /api/settings

Handle `GET` requests to `/api/settings`.


### Example

```bash
curl -X GET http://localhost:3000/api/settings
```

---

## PUT /api/settings

Handle `PUT` requests to `/api/settings`.


### Example

```bash
curl -X PUT http://localhost:3000/api/settings
```
